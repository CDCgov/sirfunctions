# Determine the size of data to pull

Determine the size of data to pull

## Usage

``` r
set_data_size(year)
```

## Arguments

- year:

  `int` start year of the desk review

## Value

`str` size of the data
