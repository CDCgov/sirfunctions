# Add province name back to those without district names

Add province name back to those without district names

## Usage

``` r
add_prov_npafp_table(npafp.output)
```

## Arguments

- npafp.output:

  tibble output after running f.npafp.rate.01 at the district level

## Value

tibble of output with province names for those without district names
