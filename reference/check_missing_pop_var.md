# Helper function to check missing variables in the pop file based on spatial scale

Helper function to check missing variables in the pop file based on
spatial scale

## Usage

``` r
check_missing_pop_var(pop_data, spatial_scale)
```

## Arguments

- pop_data:

  population dataset

- spatial_scale:

  "ctry", "prov", or "dist"
