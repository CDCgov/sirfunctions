# Helper function to check if spatial scale of data matches spatial scale chosen

Helper function to check if spatial scale of data matches spatial scale
chosen

## Usage

``` r
check_spatial_scale(admin_data, spatial_scale)
```

## Arguments

- admin_data:

  population file dataset

- spatial_scale:

  "ctry", "prov", or "dist"
