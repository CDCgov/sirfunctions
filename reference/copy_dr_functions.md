# Get functions used for the desk review from Github

Get functions used for the desk review from Github

## Usage

``` r
copy_dr_functions(branch = "main", output_folder = Sys.getenv("DR_FUNC_PATH"))
```

## Arguments

- branch:

  which branch to use

- output_folder:

  where the function scripts should be stored
