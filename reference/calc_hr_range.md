# Return an array of numbers summarized in human readable format

Given an array of numbers this returns those numbers in human readable
format.

## Usage

``` r
calc_hr_range(numbers)
```

## Arguments

- numbers:

  `int` An array of numbers.

## Value

A string in human readable format.
