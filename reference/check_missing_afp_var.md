# Helper function to check missing variables in the AFP linelist based on spatial scale

Helper function to check missing variables in the AFP linelist based on
spatial scale

## Usage

``` r
check_missing_afp_var(afp_data, spatial_scale)
```

## Arguments

- afp_data:

  AFP dataset

- spatial_scale:

  "ctry", "prov", or "dist"
